CMAKE_MINIMUM_REQUIRED(VERSION 3.10)

INCLUDE_DIRECTORIES(Public)
INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/Sources/Drivers/GL/Public)

AUX_SOURCE_DIRECTORY(./Private/Client CLIENT_SRCS)

ADD_LIBRARY(Client STATIC ${CLIENT_SRCS})
TARGET_LINK_LIBRARIES(Client GL)