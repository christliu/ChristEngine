AUX_SOURCE_DIRECTORY(./Private/Win32 WIN32_SRCS)

INCLUDE_DIRECTORIES(Public)
INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/Sources/Client/Public)

ADD_LIBRARY(Windows STATIC ${WIN32_SRCS})

TARGET_LINK_LIBRARIES(Windows Client)

